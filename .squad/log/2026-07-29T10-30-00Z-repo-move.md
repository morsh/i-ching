# Session log — repo move

- **When:** 2026-07-29T10:30:00Z
- **Summary:** I-Ching Oracle moved from the `morsh/mor-squads` collection repo into standalone `morsh/i-ching` with history preserved by subtree split. The squad state moved with the app, the app now lives at `app/`, deployment targets the `prod` branch, and `https://morsh.github.io/i-ching/` is the sole canonical Pages site.