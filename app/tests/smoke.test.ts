/**
 * Placeholder smoke test so the Vitest runner is wired up from day one.
 *
 * OWNER: Tank. Replace/expand with real engine, corpus, and UI tests.
 */

import { describe, it, expect } from 'vitest';

describe('harness', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
